export enum routes {
  ROOT = "",
  AUTH = "auth",
  MAIN = "/main-page",
  PROFILE = "/profile",
  INDICATIONS = "/indications",
  STATISTIC = "/statistic",
  REPORTS = "/reports",
  SETTINGS = "/settings",
}
